test_suite_config:
  labels: [unit, regression, integration, trunk]
  files:
    - path: ${TORCH_DEVICE_ROOT}/tests/inductor/test_sliced_staggered_convert.py
      unlisted_test_mode: mandatory_success
